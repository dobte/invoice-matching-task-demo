otree>=5.0.0a21
# psycopg2-binary (prebuilt wheels) instead of psycopg2, so `pip install`
# needs no C compiler or Postgres headers. Not used by the sqlite dev server;
# only needed if you deploy against Postgres.
psycopg2-binary>=2.8.4
